<form action="/register#message" method="post" class="m-auto" style="max-width: 500px;">
	<div class="form-floating">
		<input class="form-control" id="firstName" name="firstName" type="text" placeholder="Entrez votre prénom..." required="" value="{{ form.firstName }}">
		<label for="firstName">Prénom</label>
	</div>
	<div class="form-floating">
		<input class="form-control" id="lastName" name="lastName" type="text" placeholder="Entrez votre nom..." required="" value="{{ form.lastName }}">
		<label for="lastName">Nom</label>
	</div>
	<div class="form-floating">
		<input class="form-control" id="email" name="email" type="email" placeholder="Entrez votre E-mail..." required="" value="{{ form.email }}">
		<label for="email">Adresse e-mail</label>
	</div>
	<div class="form-floating">
		<input class="form-control" id="password" name="password" type="password" placeholder="Entrez votre mot de passe..." required="">
		<label for="password">Mot de passe</label>
	</div>
	<div class="form-floating">
		<input class="form-control" id="confirmPassword" name="confirmPassword" type="password" placeholder="Entrez la confirmation de votre mot de passe..." required="">
		<label for="confirmPassword">Confirmation du mot de passe</label>
	</div>

	<br>

	<div class="d-flex justify-content-center">
		<button class="btn btn-primary text-uppercase text-light w-100" id="submitButton" name="submitButton" type="submit">S'enregistrer</button>
	</div>

	{% if message.success is not null %}
		<p class="text-justify text-success mb-1" id="message">Votre compte a bien été créé.</p>
		<p class="text-justify text-success mt-1 mb-2">Un mail de verification vous a été envoyé, veuillez suivre les indications qui y mentionnées pour vérifier votre compte.</p>
	{% endif %}

	{% if message.error is not null %}
		<p class="text-justify text-danger" id="message">{{ message.error }}</p>
	{% endif %}

	<div class="d-flex justify-content-center mt-4 mb-4">
		<a href="/login">Se connecter</a>
	</div>

	<div class="d-flex justify-content-center mt-4 mb-4">
		<a href="/resend">Vous n'avez pas reçu votre mail de vérification ?</a>
	</div>
</form>